export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is still good to have, though v4 might handle some bits, docs say usage with postcss works this way.
    },
}
